spring:
  datasource:
    url: jdbc:mysql://localhost:3306/certificate_question
    username: ${DATABASE_NAME}
    password: ${DATABASE_PASSWORD}
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    open-in-view: false